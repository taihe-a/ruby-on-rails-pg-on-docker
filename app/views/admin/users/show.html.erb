<div class="container">
<div class="d-flex">
<p>名前：<%= session[:name] %>　</p>
<p>ID：<%= session[:user_id] %></p>
  </div>
<table class="table">
  <tbody>
        <tr>
            <th>名前</th>
            <td><%= @user.name %></td>
        </tr>
        <tr>
            <th>メールアドレス</th>
            <td><%= @user.email %></td>
        </tr>
        <tr>
            <th>管理者権限</th>
            <td><%= @user.admin %></td>
        </tr>
  </tbody>  
</table>
<%= link_to'編集',edit_admin_user_path,class:"btn btn-white"%>
<%= link_to'削除',admin_user_path(@user.id),method: :delete,data:{confirm: "本当に削除しますか？"},class:"btn btn-danger"%>
</div>